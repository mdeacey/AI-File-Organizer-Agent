GOOGLE_API_KEY="YOUR_GOOGLE_API_KEY_HERE"

# --- Script Configuration ---
# Optional: Default directory to organize (leave empty to be prompted)
DEFAULT_TARGET_DIR="/path/to/your/default/target_directory"
# Optional: Highest-level directory the script can access (defaults to user home '~' if unset)
TOP_LEVEL_ALLOWED_PATH="~"
# Optional: Set to True for verbose debugging (defaults to False if unset)
DEBUG="False"
